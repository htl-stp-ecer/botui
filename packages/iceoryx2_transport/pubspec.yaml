name: iceoryx2_transport
description: iceoryx2 shared-memory transport bindings for Dart/Flutter. Replaces LCM with zero-copy IPC for raccoon-based robotics apps.
version: 0.1.0
repository: https://github.com/htl-stp-ecer/iceoryx2-transport

environment:
  sdk: ^3.0.0

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.24.0
