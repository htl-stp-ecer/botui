name: raccoon_ring_transport
description: Dart/Flutter FFI bindings around raccoon_ring (the SHM ring-buffer transport from raccoon-transport).
version: 0.2.0

environment:
  sdk: ^3.0.0

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  test: ^1.24.0
